Это динамический вызов, принимает тип `object` делая его упаковку/распаковку
Медленнее `Invoke`
Используется когда сигнатура метода не известна на этапе компиляции
Оборачивает исключения в `TargetInvocationException`

```C#
delegate.DynamicInvoke(object[] args);
```