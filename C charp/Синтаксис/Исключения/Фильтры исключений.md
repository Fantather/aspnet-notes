- Позволяют обрабатывать исключения только если выполняется условие
- Пишется с помощью `when(Bool)`

```C#
catch (Exception ex) when (ex.Message.Contains("файл"))
{
    Console.WriteLine("Обрабатываем только ошибки с файлом");
}
```