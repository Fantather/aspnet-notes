Унарный оператор действует над одним операндом
Примеры: `+a`, `-a`, `!a`, `~a`, `++a`, `--a`

При перегрузке, компилятор сам реализует присваивание нового значения

Сигнатура имеет один параметр:
```csharp
// Унарный минус
public static MyType operator -(MyType a) { ... } 
```