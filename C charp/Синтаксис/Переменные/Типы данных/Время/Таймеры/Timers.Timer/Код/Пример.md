```C#
public class Example
{
	private static void Timer_Elapsed(object? sender, ElapsedEventArgs e)
	{
		try
		{
			Console.WriteLine("Сработало событие");
		}
		
		catch (Exception ex)
		{
			Console.WriteLine(ex);
		}
		
		finally
		{
			t.Start();
		}
	}
}

// В конце блока автоматически вызовется Dispose()
using(Timer timer = new Timer(5000))  // интервал 5 сек
{
	timer.Elapsed += Timer_Elapsed;
	timer.AutoReset = false; // true по умолчанию
	timer.Start();
	
	Console.WriteLine("Таймер запущен. Нажмите Enter для выхода.");
    Console.ReadLine();
}
```