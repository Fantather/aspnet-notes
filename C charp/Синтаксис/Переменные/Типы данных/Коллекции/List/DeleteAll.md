Метод принимающий Предикат, применяет предикат к каждому элементу списка и удаляет этот элемент, если предикат вернул `true`
Возвращает количество всех совпадений

```C#
Numbers.DeleteAll(n => n % 2 == 0);
List.DeleteAll(obj => obj.Name == nameToDelete);
```
