`Length``(int)` - текущая длинна текста, можно изменить напрямую
`Capacity` `(int)` - размер зарезервированного буфера, удваивается при переполнении
`MaxCapacity` `(int)` - Максимальный размер буфера (Заданный в конструкторе)