**Основные члены:**
- `decimal.MinValue` ≈ –7.9×10^28
    
- `decimal.MaxValue` ≈ +7.9×10^28
    
- `decimal.Zero`, `decimal.One`, `decimal.MinusOne`
    
- Методы:
    
    - `decimal.Round`, `decimal.Floor`, `decimal.Ceiling`
        
    - `decimal.Parse`, `decimal.TryParse`
        
    - Операторы `+`, `-`, `*`, `/`, `%`