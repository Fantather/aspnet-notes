*General Responsibility Assignment Software Patterns* - Это набор принципов, которые помогают решить главный вопрос проектирования:  
Как правильно распределить обязанности между классами и объектами?

---

## 1. **Information Expert (Информационный эксперт)**

**Формулировка:** назначай обязанность тому классу, который обладает необходимой информацией для её выполнения.  
**Объяснение:** если нужно посчитать стоимость заказа — пусть это делает сам `Order`, ведь у него есть список товаров и их цены.

---

## 2. **Creator (Создатель)**

**Формулировка:** класс должен создавать объект, если он:

- содержит его,
    
- использует его,
    
- хранит его,
    
- тесно с ним связан.  
    **Объяснение:** `Order` должен создавать `OrderItem`, потому что заказ «владеет» своими позициями. Не нужно плодить отдельный фабричный класс без причины.
    

---

## 3. **Controller (Контроллер)**

**Формулировка:** ответственность за обработку событий из UI назначается контроллеру, а не самому UI или бизнес-логике.  
**Объяснение:** кнопка «Оформить заказ» вызывает метод у `OrderController`, а не сама считает скидки или лезет в базу.

---

## 4. **Low Coupling (Слабая связанность)**

**Формулировка:** минимизируй зависимости между классами.  
**Объяснение:** если `Order` напрямую работает только с `ICustomer`, а не с `CustomerDbService`, его проще менять и тестировать.

---

## 5. **High Cohesion (Высокая связность)**

**Формулировка:** у класса должны быть только близкие по смыслу обязанности.  
**Объяснение:** `Invoice` отвечает только за счет-фактуру, а не за печать чеков, хранение логов и отправку email.

---

## 6. **Polymorphism (Полиморфизм)**

**Формулировка:** для вариативного поведения используй полиморфизм, а не условные конструкции.  
**Объяснение:** вместо `if (типДокумента == PDF) … else if (Word)…` — сделай абстрактный `DocumentExporter` и классы `PdfExporter`, `WordExporter`.

---

## 7. **Pure Fabrication (Чистая выдумка)**

**Формулировка:** если обязанность нельзя логично отдать «эксперту», то придумай отдельный класс, чтобы снизить связанность или повысить переиспользование.  
**Объяснение:** `Logger` — это «выдуманный» класс, его нет в предметной области, но он нужен для чистоты архитектуры.

---

## 8. **Indirection (Посредник, косвенность)**

**Формулировка:** чтобы уменьшить зависимость между компонентами, введи промежуточный объект.  
**Объяснение:** UI не должен напрямую работать с базой, лучше ввести слой `Repository` или `Service`.

---

## 9. **Protected Variations (Защита от изменений)**

**Формулировка:** изолируй изменяющиеся элементы системы за стабильным интерфейсом.  
**Объяснение:** работаешь с `IPaymentGateway`, а не напрямую с API конкретного банка. Если банк сменился — меняется только реализация, а не весь код.

---

🔹 **Итого:**  
GRASP = **9 принципов**, которые учат:

- кто должен хранить данные,
    
- кто должен их обрабатывать,
    
- как уменьшить зависимости,
    
- как защититься от изменений.
    

---

Хочешь, я соберу эти 9 принципов в **таблицу-шпаргалку** (с формулировкой + примером в одну строку), чтобы тебе было удобно повторять и сравнивать?