Нативная точка входа (PE Entry Point / AddressOfEntryPoint)  - адрес первой инструкции, которую Windows загрузчик вызовет после маппинга модуля в память
Обычно хранит машинный код, а не IL код

Хранится в заголовке PE, в структуре `IMAGE_OPTIONAL_HEADER.AddressOfEntryPoint`