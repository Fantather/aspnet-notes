![[Pasted image 20250605200300.png]]

**CLR**
Название среды — общеязыковая среда выполнения (Common Language Runtime, CLR) — говорит само за себя: это среда выполнения, которая подходит для разных языков программирования. Основные возможности CLR (управление памятью, загрузка сборок, безопасность, обработка исключений, синхронизация) доступны в любых языках программирования, использующих эту среду. Например, при об работке ошибок среда выполнения опирается на исключения, а значит, во всех языках программирования, использующих эту среду выполнения, сообщения об ошибках передаются при помощи механизма исключений. Или, например, среда выполнения позволяет создавать программные потоки, а значит, во всех языках программирования, использующих эту среду, тоже могут создаваться потоки.

## 1. Таблицы определений (Definition Tables)

Эти таблицы описывают **всё, что определено внутри этой сборки** — классы, методы, поля, свойства и т.д.

Примеры:

- **TypeDef** — описание всех типов (классов, структур, интерфейсов), которые ты сам объявил.
    
- **MethodDef** — список методов, которые ты реализовал.
    
- **FieldDef** — список всех полей классов.
    
- **Property**, **Event** и другие — описания соответствующих членов.


---

## 🔷 2. Таблицы ссылок (Reference Tables)

Эти таблицы описывают **всё, на что твой код ссылается**, но не определяет сам. То есть — **внешние типы, методы, сборки** и т.д.

Примеры:

- **AssemblyRef** — список сборок, на которые ты ссылаешься (например, `System.Console` из `System.Console.dll`)
    
- **TypeRef** — описание внешних типов, к которым есть обращения.
    
- **MemberRef** — ссылки на методы или поля, определённые вне этой сборки.