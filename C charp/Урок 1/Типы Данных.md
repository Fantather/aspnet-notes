![[Pasted image 20250605200106.png]]

![[Pasted image 20250605202031.png]]
Типы данных делятся на value type и reference type, у которых общий родитель - object

reference type - Типы для указателей, сохраняются в Heap
Value type - Типы для значений, сохраняются в Stack


---

В C# все типы делятся на **Value Types (значимые типы)** и **Reference Types (ссылочные типы)**. Это ключевая концепция, влияющая на то, **как данные хранятся в памяти, копируются и передаются в методы**.

---

## 🔷 Value Types (значимые типы)

> Переменная содержит **само значение**, а не ссылку на него.

### ✅ Примеры:

- Простые типы: `int`, `float`, `bool`, `char`
    
- Структуры: `struct`, включая `DateTime`, `TimeSpan`
    
- Перечисления: `enum`
    

### 🧠 Поведение:

```csharp
int a = 5;
int b = a;  // b копирует значение a

b = 10;

Console.WriteLine(a);  // 5
Console.WriteLine(b);  // 10
```

> Каждая переменная — отдельная копия данных.

---

## 🔷 Reference Types (ссылочные типы)

> Переменная содержит **ссылку (указатель)** на объект в **куче (heap)**, а не само значение.

### ✅ Примеры:

- `string`
    
- Классы (`class`)
    
- Массивы (`int[]`, `string[]`, и т.д.)
    
- Интерфейсы (`interface`)
    
- Делегаты (`delegate`)
    
- `object`
    

### 🧠 Поведение:

```csharp
class Person
{
    public string Name;
}

Person p1 = new Person { Name = "Alice" };
Person p2 = p1;  // p2 и p1 указывают на один и тот же объект

p2.Name = "Bob";

Console.WriteLine(p1.Name);  // "Bob"
```

> Обе переменные ссылаются на **один и тот же объект** в памяти.

---

## 📊 Таблица сравнения:

|Свойство|Value Type|Reference Type|
|---|---|---|
|Где хранится|В стеке (stack)|В куче (heap), ссылка — в стеке|
|Что хранит переменная|Само значение|Ссылку на объект|
|Передача в метод|По значению (копия)|По значению (копия ссылки)|
|Может быть `null`|❌ (только через `Nullable<T>`)|✅|
|Поведение при присваивании|Копирование значения|Копирование ссылки|
|Производительность|Быстрее|Медленнее (из-за работы с кучей)|
|Примеры|`int`, `bool`, `double`, `struct`|`class`, `string`, `object`, `array`|

---

## 📌 Особенность: `ref struct` и `readonly struct`

C# добавил расширения для **значимых типов**, чтобы они могли быть:

- неизменяемыми (`readonly struct`)
    
- использовать стек только (`ref struct` — как `Span<T>`)
    

---

## 🔄 Пример: разница при передаче в метод

```csharp
void ChangeValue(int x) { x = 100; }
void ChangeRef(Person p) { p.Name = "Changed"; }

int val = 5;
Person obj = new Person { Name = "Original" };

ChangeValue(val);
ChangeRef(obj);

Console.WriteLine(val);       // 5 — копия не изменила оригинал
Console.WriteLine(obj.Name);  // "Changed" — ссылка на тот же объект
```

---

Хочешь — могу сделать визуальные схемы или привести аналогии с коробками или указателями.