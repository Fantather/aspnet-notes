**Реляционные базы данных (РБД)** — это тип баз данных, в которых данные организованы в виде набора взаимосвязанных **таблиц** (называемых также **отношениями** — от англ. *relation*, что и дало название модели).

Это самая распространенная и популярная модель баз данных на сегодняшний день, основанная на строгой математической теории — реляционной алгебре.

---

### Ключевые принципы реляционных БД:

#### 1. Данные хранятся в таблицах (отношениях)
Каждая таблица представляет собой один объект предметной области (например, `Пользователи`, `Заказы`, `Товары`).

*   **Столбцы (Атрибуты):** Определяют, какая информация хранится в таблице. У каждого столбца есть уникальное имя и тип данных (например, `id` (целое число), `name` (текст), `birthdate` (дата)).
*   **Строки (Кортежи):** Это конкретные записи или экземпляры данных. Каждая строка представляет собой один набор связанных значений (например, данные одного пользователя).

**Пример таблицы `Users`:**
| id | name | email | city_id |
| :--- | :--- | :--- | :--- |
| 1 | Иван | ivan@mail.ru | 1 |
| 2 | Мария | maria@yandex.ru | 2 |
| 3 | Петр | petr@gmail.com | 1 |

#### 2. Связи между таблицами
Это самый важный признак реляционной модели. Таблицы не изолированы; они связаны между собой с помощью **ключей**.

*   **Первичный ключ (Primary Key, PK):** Уникальный идентификатор каждой строки в таблице (например, `id` в таблице `Users`). Не может быть пустым или повторяться.
*   **Внешний ключ (Foreign Key, FK):** Столбец в одной таблице, который ссылается на первичный ключ в другой таблице. Он обеспечивает логическую связь между данными.

**Пример связи:**
У нас есть вторая таблица `Cities`:
| id | name |
| :--- | :--- |
| 1 | Москва |
| 2 | Санкт-Петербург |

Поле `city_id` в таблице `Users` — это **внешний ключ**, который ссылается на поле `id` (первичный ключ) в таблице `Cities`. Это позволяет однозначно определить, в каком городе живет каждый пользователь, не храня название города прямо в таблице пользователей (это устраняет избыточность).

#### 3. Обеспечение целостности данных
Реляционные СУБД строго следят за соблюдением правил:
*   **Целостность сущностей:** Каждая таблица должна иметь первичный ключ.
*   **Ссылочная целостность:** Значение внешнего ключа должно либо быть пустым (NULL), либо соответствовать существующему значению первичного ключа в связанной таблице. Нельзя удалить город из таблицы `Cities`, если на него ссылается хотя бы один пользователь в таблице `Users` (либо нужно определить каскадное удаление).
*   **Целостность по правилам:** Можно задавать свои правила (ограничения), например, «возраст не может быть отрицательным», «email должен содержать символ @».

---

### Как с ними работают? С помощью SQL!

Для управления реляционными БД используется универсальный язык структурированных запросов — **SQL (Structured Query Language)**. С его помощью:
*   `SELECT * FROM Users;` — **получить** данные.
*   `INSERT INTO Users ...` — **добавить** данные.
*   `UPDATE Users SET ...` — **изменить** данные.
*   `DELETE FROM Users ...` — **удалить** данные.
*   `CREATE TABLE ...` — **создать** структуру (таблицу).

---

### Преимущества и недостатки

**👍 Преимущества:**
*   **Отсутствие избыточности:** Данные хранятся в нормализованном виде (каждый факт — в одном месте).
*   **Гибкость:** С помощью SQL-запросов можно получать очень сложные выборки данных из нескольких таблиц.
*   **Целостность:** Встроенные механизмы гарантируют точность и непротиворечивость данных.
*   **Надежность:** Поддержка транзакций (принцип ACID). Транзакция — это атомарная операция («всё или ничего»). Если одна часть операции fails, откатывается вся операция.

**👎 Недостатки:**
*   **Сложность масштабирования:** Классическое вертикальное масштабирование (увеличение мощности сервера) может быть дорогим. Горизонтальное масштабирование (разделение базы на несколько серверов) сложнее, чем у NoSQL-систем.
*   **Производительность на больших объемах:** Для сверхбольших данных и высоких нагрузок (например, социальные сети) могут потребоваться сложные оптимизации.
*   **Сложность схемы:** Необходимость тщательного проектирования структуры (схемы) заранее. Изменение схемы на больших проектах может быть трудозатратным.

---

### Итог

**Реляционная БД — это упорядоченный набор таблиц, связанных между собой через ключи, управляемый с помощью SQL и обеспечивающий целостность и надежность данных.**

**Популярные реляционные СУБД:** MySQL, PostgreSQL, Oracle Database, Microsoft SQL Server, SQLite.

Они идеально подходят для большинства бизнес-приложений, где важна точность, надежность и сложные связи между данными (интернет-магазины, банковские системы, системы учета).