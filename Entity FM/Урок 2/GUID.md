Уникальный идентификатор для объекта
Повышает защищенность таблицы за счёт неожидаемого, рандомизированного идентификатора


`GUID` не генерируется автоматически,
Для генерации уникального значения можно наипсать
```C#
public Guid Id { get; set; } = Guid.NewGuid();
```

