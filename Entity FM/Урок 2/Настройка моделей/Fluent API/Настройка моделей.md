![[Pasted image 20250904200322.png]]

`HasKey` - объявляем о том что у таблицы есть `PRIMARY KEY`
`HasAlternateKey` - дополнительный ключ

`ToTable` - 
`HasCheckConstraint` - накладываем Запрет
Лучше прописывать их для всего класса
- `CK_Quantity` - название запрета
- `Quantity>=1` - сам запрет

`ApplyConfiguration` - Принимаем настройки указанные ниже


![[Pasted image 20250904200736.png]]
![[Pasted image 20250904201507.png]]
Если у нас много сущностей

`.HasName()` - указываем имя запрета
`.HasColumnName()` - указываем имя для столбца
`.IsRequired` - может быть `NULL`
`.HasColumnType` - Указываем тип для колонки
`.HasMaxLength(30)` - ограничиваем длину, по умолчанию максимум
`HasCheckConstraint` - накладываем Запрет на весь класс

