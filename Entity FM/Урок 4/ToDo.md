Зачем в коде С# при связи один к многим дочерние объекты (Многие) хранят в себе и `id`(`CategoryId`) рядка в родительском классе и навигационное свойство (`Category category`) одновременно?

Вариант 3 изучить самостоятельно

Ссылка на примеры
[AliceBit2020/RelationDB_EFCore](https://github.com/AliceBit2020/RelationDB_EFCore)


ДЗ
Построить описанную БД Академия на стороне C#