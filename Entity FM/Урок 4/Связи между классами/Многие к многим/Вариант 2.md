![[Pasted image 20250909201247.png]]

Этот вариант легче в плане добавления объектов
В связываемых классах прописываем свойство хранящее коллекцию объектов Промежуточной таблицы
Эти объекты промежуточной таблицы хранят в себе ссылки на оба связываемых объекта и их `id`, то есть выступают в виде рядка промежуточной таблицы

И через FluentAPI указывается что между объектами связь Многие к Многим