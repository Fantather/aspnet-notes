![[Pasted image 20251204182358.png]]


`express` - обработчик запросов клиентов
Он упрощает работу с серверным JS кода

**Настройки**
`app.use(express.urlencoded({ extended: true }));`
`app.use(express.json);`
`app.use(express.static('public'));`