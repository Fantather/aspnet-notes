механизм для внешнего мира

это специальные члены класса, которые предоставляют гибкий механизм для чтения, записи или вычисления значений закрытых полей (private fields).

```c#
public class Person
{
    // Компилятор автоматически создает скрытое поле для Name
    public string Name { get; set; } 
}
```
___

компоненты могут получать свойства и отображаться по разному
элемент представляющий ссылку и по разному ведет себя в зависимости от атрибута 
свойства объекта зависят от свойства `this.props`

Что бы при вызове классового компонента передать какие то свойства
![[Pasted image 20251210184459.png]]
если мы хотим сюда что то передать и что то использовать - нам нужен this 

![[Pasted image 20251210184528.png]]
через `this.props` нам будут доступны любые параметры которые мы добавили

# как их передать?
![[Pasted image 20251210184751.png]]

```jsx
  

function MyComponent(props) {

  return <div>Im func component my name is - {props.name}</div>;

}

  

class MyClassComponent extends React.Component {

  render() {

    return <div>Im func component! My name is - {this.props.name}</div>;

  }

}

  

export default App;
```

в функциональном компоненте объект пропс должен быть как параметр
____
