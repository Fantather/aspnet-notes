### 🔹 **zod / yup** (часто с React)

#### Пример с `zod`
`npm install zod`

```JSX
import { z } from 'zod';  const schema = z.object({   
	login: z.string().min(3).max(20).regex(/^[a-zA-Z0-9_]+$/),   
	password: z.string().min(8),   
	confirmPassword: z.string() }).refine(data => data.password === data.confirmPassword, {   message: "Пароли не совпадают",   path: ["confirmPassword"] 
});
```

🔥 идеально для форм + TypeScript

---

## 4️⃣ Главное правило (очень важно)

🚨 **Никогда не полагайся только на фронтенд**

- фронт → для UX
    
- сервер → **обязательная повторная валидация**
    
- пароль → **хешировать (bcrypt, argon2)**