- *Props* (свойства): Начинаются с `on` (пример: `onImageSelect`, `onClick`)
- *Обработчики* (функции): Начинаются с `handle` (пример: `handleUpdateAvatar`)

Называйте обработчики по их **цели** (намерению), а не по механическому действию (лучше `handleLogin`, чем `handleButtonClick`)