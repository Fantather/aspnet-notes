В React данные передаются только **вниз** (от родителя к ребенку через `props`). Ребенок не может напрямую изменить данные родителя.

Чтобы ребенок мог "попросить" родителя что-то сделать, используется паттерн **"Callback function"** (функция обратного вызова).

**Принцип работы (Аналогия с пультом):**

1. **Родитель** (телевизор) содержит логику и состояние.
    
2. **Родитель** создает функцию (`handle...`) и передает её **Ребенку** через пропсы.
    
3. **Ребенок** (пульт) — глупый компонент. Он просто получает функцию и вызывает её, когда пользователь нажимает кнопку.

**Схема именования:**

- В Родителе функция называется по действию: `handleDeleteUser` (что произойдет).
- В пропсах Ребенка она называется как событие: `onDelete` (когда это случится).


#### Пример кода

Представь **Карточку профиля** (Родитель), внутри которой лежит кнопка **Сохранить** (Ребенок).


```JavaScript
// 1. Дочерний компонент (Глупый, просто кнопка)
// Он не знает, что именно произойдет. Его работа — сообщить о клике.
const SaveButton = ({ onSave }) => {
  return (
    <button onClick={onSave}>
      💾 Сохранить изменения
    </button>
  );
};

// 2. Родительский компонент (Умный, владеет данными)
const UserProfile = () => {
  // Логика находится здесь
  const handleUpdateProfile = () => {
    console.log("Отправляем данные профиля на сервер...");
  };

  return (
    <div className="profile">
      <h1>Мой профиль</h1>
      {/* Мы передаем "пульт управления" (функцию) ребенку */}
      <SaveButton onSave={handleUpdateProfile} />
    </div>
  );
};
```

**Разбор потока:**

1. Пользователь кликает на `<button>` в компоненте `SaveButton`.

2. Срабатывает `onClick`, который вызывает props `onSave`.

3. По ссылке вызывается функция `handleUpdateProfile` в родителе `UserProfile`.

4. Родитель выполняет логику.




---
## Что возвращает системный Тег и кастомный

Это отличный вопрос! Именно здесь кроется разница между стандартным HTML-элементом и вашим собственным React-компонентом.

Давайте представим `TypesSelect` как посредника или переводчика.

1. Внутри TypesSelect (HTML тег `<select>`):
    
    Браузер создает событие event (объект с кучей технических деталей).
    
    Вы пишете: `(e) => onChange(e.target.value)`
    
    Здесь вы говорите: "Возьми этот сложный объект e, достань из него только текст (например, 'cow') и выбрось всё остальное, передав дальше только текст".
    
1. Снаружи в FarmSimulator (Ваш компонент `<TypesSelect>`):
    
    Ваш компонент получает то, что ему передал "переводчик". Так как "переводчик" внутри уже отфильтровал событие и оставил только строку, родитель получает именно строку.
    

Давайте проверим это на мысленном эксперименте.

Если бы вы внутри `TypesSelect` написали такой код:

```JS
// Внутри TypesSelect
<select onChange={(e) => onChange(555)}>
```

Как вы думаете, чему была бы равна переменная `value` в родительском компоненте `FarmSimulator` в этом случае?