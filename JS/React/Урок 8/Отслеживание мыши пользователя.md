```JSX
import React from "react";

function App() {

  return (
    <div>
      <h3>Move your mouse in the gray area:</h3>
      {/* 1. App передаёт логику отображения (анонимную функцию)
             компоненту MouseTracker через prop 'render'.
          2. Эта функция принимает данные (coords) от MouseTracker.
      */}

      <MouseTracker
        render={(coords) => (
          <p style={{ position: "absolute", top: coords.y, left: coords.x }}>
            👀 ({coords.x}, {coords.y})
          </p>
        )}
      />
    </div>
  );
}

export default App;

// Компонент-Провайдер Логики
function MouseTracker({ render }) {
  // Принимает функцию как prop с именем 'render'
  const [position, setPosition] = React.useState({ x: 0, y: 0 });
  const handleMouseMove = (event) => {

    setPosition({
      x: event.clientX,
      y: event.clientY,
    });
  };

  return (
    <div
      style={{ height: "100vh", border: "2px solid gray" }}
      onMouseMove={handleMouseMove}
    >

      {/* Здесь мы вызываем функцию, переданную в props.render,
          и передаём ей данные (position) в качестве аргумента.
          Функция вернёт JSX для отображения.
      */}
      {render(position)}
    </div>
  );
}
```