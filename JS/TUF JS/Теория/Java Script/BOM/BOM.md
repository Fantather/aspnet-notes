**BOM (Browser Object Model)** — это **набор объектов, через которые JavaScript управляет браузером**, а не содержимым страницы.

Если коротко:

- **DOM** — работает с HTML-страницей
    
- **BOM** — работает с **окном браузера и его возможностями**
    

---

## Главное отличие

- **DOM** → `document`, теги, кнопки, текст
    
- **BOM** → `window`, вкладки, адрес, размер окна, диалоги
    

---

## Основной объект BOM — `window`

`window` — это **главный объект браузера**, всё остальное внутри него.

Примеры объектов BOM:

### 🔹 `window.location`

Работа с адресной строкой

```js
console.log(location.href); // текущий URL
location.href = "https://google.com"; // переход
```

---

### 🔹 `window.history`

История переходов

```js
history.back();    // назад
history.forward(); // вперёд
```

---

### 🔹 `window.navigator`

Информация о браузере

```js
navigator.userAgent;
```

---

### 🔹 `window.screen`

Информация об экране

```js
screen.width;
screen.height;
```

---

### 🔹 Диалоги

```js
alert("Привет");
confirm("Ты уверен?");
prompt("Введи имя");
```

---

## Важно знать

- BOM **не является стандартом**, как DOM, но поддерживается всеми браузерами
    
- Обычно `window.` **не пишут**, потому что он глобальный:
    

```js
alert("Hi"); // это window.alert
```

---

### Итог

**BOM — это всё, что связано с браузером, а не с HTML.**

Если хочешь, могу:

- сравнить BOM и DOM на примерах
    
- показать, где BOM реально используют в проектах
    
- объяснить, почему в React ты почти не работаешь с BOM напрямую