```JS
var createClub = (pName, pCity, pYear) => (
	{
		name: pName,
		city: pCity,
		year: pYear,
		displayInfo: function ()
		{
			document.write(
			"<br />Название: " + this.name +
			"<br />ropoA: " + this.city +
			"<br />Год основания: " + this.year + "<br/>");
		}
	}
);

createClub
```

Эти функции при создании привязываются к ближайшему локальному окружению