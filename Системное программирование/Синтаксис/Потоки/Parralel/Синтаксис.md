```C#
Parralel.For(0, N, Method);
Parralel.For(0, 100, Method);
Parralel.For(0, N, delegate (int i));
Parralel.For(0, N, (i) => Console.WriteLine(i));
```

`0` - Начало цикла
`N` - количество повторений переданного метода, не включительно
Каждый метод попытается запуститься на отдельном ядре