В .NET _сборка_ - это единица развертывания и исполнения (обычно `.dll` или `.exe`)
Внутри - скомпилированный код и манифест (метаданные о версиях, зависимостях)

_С помощью `Assembly` можно:_
- загрузить сборку в память во время работы программы (`Load`, `LoadFrom`)
  
- узнать её версии, зависимости (`GetReferencedAssemblies`)
  
- найти и создать экземпляры типов, даже если их имена известны только в runtime (`CreateInstance`)

💡 **Когда применяют:**  
Например, плагиновая система: ты подгружаешь внешние библиотеки без перекомпиляции основной программы


Использует пространство имён `System.Reflection`
